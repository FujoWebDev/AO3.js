// I'll move this to the right spot, just a temporary thing to get a draft PR going
interface IgetAuthorWorks {
  username: string;
  page: number;
}

export const getAuthorWorks = ({ username, page = 0}) => {

}
